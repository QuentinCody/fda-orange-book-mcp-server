{
  "name": "fda-orange-book-mcp-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8849",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "lint": "biome lint --diagnostic-level=error src/",
    "type-check": "npm run cf-typegen && tsc --noEmit",
    "test": "npm run type-check"
  },
  "dependencies": {
    "@bio-mcp/shared": "workspace:*",
    "@cloudflare/codemode": "^0.1.1",
    "agents": "0.20.1",
    "fflate": "^0.8.2",
    "zod": "^4.3.6",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.2.1",
    "typescript": "5.8.3",
    "wrangler": "^4.56.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
